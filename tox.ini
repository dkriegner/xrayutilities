[tox]
envlist = py{36,37,38}
distshare = {toxinidir}/dist

[testenv]
wheel = true
passenv = TRAVIS
deps =
    h5py
    lmfit
    matplotlib
    numpy
    setuptools
    scipy
    pytest
    pytest-cov
commands = pytest --cov
