[build-system]
requires = ["setuptools", "wheel", "numpy"]
build-backend = "setuptools.build_meta"

[tool.coverage.paths]
source = [
    "lib",
    "*/site-packages"
]

[tool.coverage.run]
branch = true
parallel = true
source = [
    "xrayutilities"
]
